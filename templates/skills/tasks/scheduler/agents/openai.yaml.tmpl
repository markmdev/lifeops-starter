interface:
  display_name: "Task Scheduler"
  short_description: "Dispatch scheduled personal tasks from markdown schedule and log files"
  default_prompt: "Use $scheduler to read the schedule and run log, decide what is due right now, run only due task skills, append log entries, and stop silently if nothing is due."

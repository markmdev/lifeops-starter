interface:
  display_name: "Messages Attention"
  short_description: "Read-only Messages triage that persists message-derived CoS state"
  default_prompt: "Use $messages-attention to review new local Messages activity in read-only mode, persist concrete actions or decisions, notify only on clear attention items, or retrieve a specific attachment when requested."

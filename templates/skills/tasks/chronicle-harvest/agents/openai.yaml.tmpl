interface:
  display_name: "Chronicle Harvest"
  short_description: "Harvest candidate context from Chronicle screen history"
  default_prompt: "Use $chronicle-harvest to review new Chronicle summaries since the last checkpoint, append conservative candidate signals to the machine outbox, update state, and notify CoS only for high-signal items or blockers."

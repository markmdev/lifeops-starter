interface:
  display_name: "Operator Executor"
  short_description: "Execute eligible Operator tasks and scheduled checks"
  default_prompt: "Use $operator-executor to run the Operator execution loop: check due scheduled tasks, complete eligible autonomous work, prepare assisted work, update Operator state, and notify only when {{USER_NAME}} or {{COS_NAME}} needs attention."

interface:
  display_name: "Inbox Attention"
  short_description: "Watch new Gmail, reconcile drafts, and persist email-derived CoS state"
  default_prompt: "Use $inbox-attention to check only new relevant Gmail, reconcile active draft state, persist durable actions and decisions, and stay silent unless {{USER_NAME}} needs attention or a draft is ready."

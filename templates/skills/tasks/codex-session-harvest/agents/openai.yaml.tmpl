interface:
  display_name: "Codex Session Harvest"
  short_description: "Harvest candidate context from Codex sessions"
  default_prompt: "Use $codex-session-harvest to review new local Codex sessions since the last checkpoint, append conservative candidate signals to the machine outbox, update state, and notify CoS only for high-signal items or blockers."
